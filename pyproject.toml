[project]
name = "10k-2026"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lol", email = "lol@lol.ru" }
]
requires-python = ">=3.8"
dependencies = [
    "numpy>=1.24.4",
]

[project.scripts]
10k-2026 = "10k_2026:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
